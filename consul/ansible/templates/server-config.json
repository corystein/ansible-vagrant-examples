{
    "server": true,
    "ui": true,
    "advertise_addr": "{{ ansible_eth1.ipv4.address }}",
    "client_addr": "{{ ansible_eth1.ipv4.address }}",
    "data_dir": "/tmp/consul",
    "bootstrap_expect": 1
}